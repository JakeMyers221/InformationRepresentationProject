CREATE TABLE Vehicle
(
ID INT AUTO_INCREMENT,
Make VARCHAR(20),
Model VARCHAR(50) NOT NULL,
Price DECIMAL(8,2),
PRIMARY KEY (ID)
) ENGINE=INNODB;
We can put in any
table constraints



CREATE TABLE Customer (ID INT, Name VARCHAR(20) NOT NULL, Address VARCHAR(50), Phone VARCHAR(14)) ENGINE=INNODB

CREATE TABLE Book (ID INT, Title VARCHAR(50) NOT NULL, Price DECIMAL(6, 2), PRIMARY KEY (ID)) ENGINE=INNODB;